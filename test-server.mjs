import https from "https"; const test = () => new Promise((resolve) => { https.get("https://api.bitget.com/api/v2/spot/market/tickers?symbol=BTCUSDT", (r) => { let d = ""; r.on("data", c => d += c); r.on("end", () => { const j = JSON.parse(d); console.log("code:", j.code); console.log("price:", j.data[0].lastPr); resolve(); }); }).on("error", e => { console.error("FAILED:", e.message); resolve(); }); }); await test(); setInterval(() => {}, 60000);
